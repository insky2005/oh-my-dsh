# 复制为 deploy/.env 并填写 / copy to deploy/.env and fill in
# 密钥只引用不内联 / secrets are referenced, never inlined
IMAGE_REPO={{imageRepo}}
IMAGE_TAG={{imageTag}}
# DEPLOY_SSH_KEY=/path/to/key   # 远程部署 SSH 私钥 / remote deploy ssh key
# REMOTE_HOST=                  # 远程主机（空=本机）/ remote host (empty = local)
# SSH_USER={{sshUser}}
