---
title: oh-my-dsh 仓库知识库
tags: [wiki, index, oh-my-dsh]
updated: 2026-09-23T12:13:33Z
sources: [platforms/macos/src/SkillsPanel.swift, platforms/macos/src/SkillsCore.swift, platforms/macos/src/SkillSources.swift, docs/skills-manager-design.md, tests/skills-panel/, platforms/macos/src/DshWebCookieJanitor.swift, platforms/macos/src/ShellConfig.swift, tests/shell-config/, tests/dsh-auth-cookies/, docs/browser-blank-panel-fix.md, README.md, CHANGELOG.md, SECURITY.md, .dsh/skills/repo-knowledge/SKILL.md, core/lib/review-log.js, core/bin/ohmy-core.js, platforms/macos/src/ReviewPanel.swift, platforms/macos/src/ReviewLogModel.swift, tests/review-panel/, docs/review-panel-design.md, platforms/macos/swift-sources.sh, platforms/macos/src/SkillInstaller.swift, docs/builtin-skills-design.md, scripts/github-publish.sh, platforms/macos/src/main.swift, platforms/macos/src/FilePanel.swift, platforms/macos/src/CodeEditorView.swift, platforms/macos/src/vendor/Highlightr/, platforms/macos/src/BrowserPanel.swift, platforms/macos/src/BrowserAPI.swift, platforms/macos/cef/CEFShim.h, platforms/macos/cef/CEFShim.mm, platforms/macos/build-cef.sh, platforms/macos/src/IssueRunnerPanel.swift, platforms/macos/src/ChannelPanel.swift, core/lib/issues.js, core/lib/jobqueue.js, core/lib/tasks.js, core/lib/channel.js, core/lib/channel-runner.js, core/lib/channel-commands.js, core/lib/channel-store.js, core/lib/dingtalk.js, core/lib/dingtalk-stream-transport.js, core/lib/dingtalk-access.js, core/lib/dingtalk-device.js, core/tests/issues.test.js, core/tests/channel-runner.test.js, docs/git-workflow.md, docs/channel-design.md, docs/channel-commands.md, docs/channel-status.md, docs/channel-storage.md, docs/channel-project-switch.md, docs/plans/BROWSER_PLAN-browser-panel.md, docs/plans/PREVIEW_PLAN-file-panel.md, scripts/version.sh, scripts/local-release.sh, scripts/release-checksums.sh, scripts/github-publish.sh, platforms/macos/src/DshWebRPC.swift, core/lib/dsh-rpc.js, core/lib/workspace-store.js, Jenkinsfile, .github/workflows/release.yml, docs/devtools-drag-fix.md, docs/dsh-version-impact.md, platforms/macos/src/WorkspaceTabMemory.swift, platforms/macos/src/PanelSurface.swift, platforms/macos/src/FilePanelTreeMenu.swift, platforms/macos/src/OpenWithApps.swift, platforms/macos/src/ImagePreviewView.swift, platforms/macos/src/ImageZoom.swift, platforms/macos/src/EditorLoadPolicy.swift, platforms/macos/src/TerminalWorkspaceTabs.swift, docs/ui-color-scheme.md, docs/ux-feedback.md, tests/file-panel/, tests/terminal-panel/, CONTRIBUTING.md]
manual: false
---

# oh-my-dsh 仓库知识库

一句话简介：**oh-my-dsh** 是把 DeepSeek Harness 的 Web 界面（`dsh web`）封装成 macOS 原生 App 的自包含壳层（WKWebView 壳 + 内置 Node/npm/dsh 运行时），不改动任何 DeepSeek Harness 源码。

## 分节页

- [overview](overview.md) — 技术栈、目录布局、构建/运行/测试方式
- [architecture](architecture.md) — 分层、模块依赖、关键数据流、部署形态
- [data-model](data-model.md) — 核心数据模型与领域概念（UserDefaults 键、RPC 信封、wiki frontmatter 等）
- [conventions](conventions.md) — 工程约定（不改源码原则、L10n、编译清单、QA 钩子、版本管理）
- [tasks](tasks.md) — 常见任务手册（构建/打包/测试/加面板/排查）

## 模块页

- [main（壳层核心）](modules/main.md) — 服务管理、升级、菜单、窗口与右栏插槽
- [projects-panel（项目面板）](modules/projects-panel.md) — **活动栏首位 / ⌥⌘P**：工作区 = projects 根目录（默认 `$DSH_HOME/oh-my-dsh/projects`，`shell/config.json` 的 `projectsRoot` 可改）下的一个子目录；面板内只输目录名即建（`mkdir` + dsh `workspace/create` **幂等注册**，注册失败只标「未注册」、不回收目录）；卡片三行 + **文件/终端/知识库/任务/通道/审查** 六个快捷入口 + 新会话 + 在 Finder 中显示 + 复制路径；「新会话」= 点 dsh web 侧栏该工作区行自带的 `+`（桥 `__dshNewSession`：dsh 复用该工作区已有的空会话、没有才新建并打开，**不重连、不堆空会话**）；点卡片名 = 「在 dsh 中打开」（复用该工作区**最近一条可打开的会话**——`blank` 空会话在侧栏不可见故跳过，running 优先，没有则走「新会话」）；六个入口与「新会话」都走同一个重根原语 `adoptProjectDirectory`（唯一真相 `ProjectDirectory.current`）
- [preview-panel（预览面板）](modules/preview-panel.md) — 文件/文件夹预览、项目目录树（回滚基线 + 共享 UI 组件，含 2026-09-21 新增的 `PanelMenuButton` 头部菜单按钮；现行实现为 file-panel）
- [file-panel（文件面板）](modules/file-panel.md) — 预览强化：无后缀/点文件按文本预览、文件内编辑 + 行号、Highlightr **分块**语法高亮（大文件不再关配色）、**目录树右键菜单**（添加到对话 / 新建/重命名/删除/在 Finder 中显示）、**头部菜单按钮**（打开项目 ▾ / 打开文件 ▾，外部应用目录）、**图片预览自适应 + 缩放**、**目录树宽度跨关闭恢复**、**页签按工作区记忆/恢复**
- [terminal-panel（终端面板）](modules/terminal-panel.md) — PTY 会话 + ANSI/VT 模拟器 + **页签按 workspace 隔离**（隐藏不杀会话、切回恢复选中）+ 双击选词/三击选行与**选中即复制**（`terminal.autoCopy`）+ 滚动方向对齐面板语义
- [wiki-panel（Repo Wiki 面板）](modules/wiki-panel.md) — 知识库生成/维护/浏览
- [issue-runner-panel（任务面板）](modules/issue-runner-panel.md) — GitHub issue 驱动的串行任务流水线（切分支→修复→推送→PR）
- [channel-panel（通道面板）](modules/channel-panel.md) — 微信 ClawBot（官方 iLink 协议）+ **钉钉（dingtalk-stream 原生适配器，PR #39）** 扫码登录 + 收消息 + 斜杠指令 / #tag 路由 + Channel–Message–Session 关联模型（会话复用/路由统一/工作区归属/全局存储/面板项目视图）+ **项目开关门控**（PR #30：通道↔workspace 启用关联存全局 `~/.dsh/channels/<channelId>.workspaces.json`，未启用回「该项目未启用该通道」/不建会话）+ 异步应答忙门 + sendTyping（微信）/文字 ack（钉钉）+ **钉钉 /bind owner-binding 安全门**；core 层（channel.js / runner / commands / store / sessions / weixin-clawbot / dingtalk-* / session-driver）+ 面板 v2（引导卡片/扫码向导/项目视图/状态徽标）+ ChannelStoreReader
- [browser-panel（浏览器面板）](modules/browser-panel.md) — 多标签 CEF/Chromium 浏览器（OSR 离屏渲染帧回调自绘）+ localhost REST API（Agent 驱动）+ Chromium 原生 DevTools 窗口 + QA 端点
- [review-panel（审查面板）](modules/review-panel.md) — **只读**变更审计（PR #44）：读 dsh 会话日志（zstd 多帧）按 会话→对话→文件→变更内容 树展示，core `review-log` + 面板 `ReviewPanel/ReviewLogModel`
- [skills-panel（技能面板）](modules/skills-panel.md) — **查找 / 安装 / 移除 agent 技能 + 调用开关**（PR #50）：扫 dsh 四个技能根并按 rank 去重标级别（内置只读 / 共享级可改开关不可移除 / 用户级·项目级可改可删），开关写回 `SKILL.md` frontmatter（切回默认即删键还原字节），registry 可配置（skills.sh 搜索 / well-known 清单 / GitHub 仓库清单），改完经 offline→online nudge 让 dsh web 立即生效
- [skill-installer（内置 Skill 全局安装器）](modules/skill-installer.md) — App 启动安装内置 skill 到全局 $DSH_HOME/skills/（重命名/缺失即装/受管更新/旧名迁移），配套测试 tests/skills/
- [build-scripts（构建与打包脚本）](modules/build-scripts.md) — platforms/macos/build-app.sh / platforms/macos/make-pkg.sh / MakeIcon.swift / build-cef.sh + release/CI 脚本（version.sh / local-release.sh / release-checksums.sh / github-publish.sh / Jenkinsfile）

## 统计

- 页面数：20（含本页；模块页 14 个）
- 主要源码：`platforms/macos/src/`（**35 个 Swift 文件**：main/PreviewPanel/FilePanel/**FilePanelTreeMenu/OpenWithApps/ImagePreviewView/ImageZoom/EditorLoadPolicy**/**ProjectsCore/ProjectsPanel/ComposerReference/SnapshotModel/SnapshotWindow**/CodeEditorView/WorkspaceTabMemory/TerminalPanel/**TerminalWorkspaceTabs**/WikiPanel/IssueRunnerPanel/ChannelPanel/ChannelStoreReader/ReviewPanel/ReviewLogModel/SkillsPanel/SkillsCore/SkillSources/PanelSurface/BrowserPanel/BrowserAPI/BrowserCDP/DshWebCookieJanitor/DshWebRPC/ShellConfig/SkillInstaller/MakeIcon）+ `platforms/macos/src/vendor/Highlightr/`（Vendored 语法高亮组件）+ `platforms/macos/cef/`（CEFShim.h/.mm ObjC++ 桥）+ 共享核心 `core/`（Node 模块，含 issues/jobqueue/tasks/channel/review-log 后 **233 用例单测**（2026-09-12 本机实测：230 通过 / 3 跳过——无 zstd 的 Node 20 上 zstd 用例自动 skip））
- **无头 Swift 单测套件（`tests/*/run.sh`，共 17 套）**：wiki-panel / terminal-panel / browser-panel / **shell-config**（旧 UserDefaults 迁移，13 例）/ skills / **skills-panel**（技能面板：模型 + 控制器冒烟 + 真实绘制回归，**121 例**）/ channel-panel / dsh-rpc（**57 例**，含 DshWorkspaceOps 17 例，其中 3 例守「blank 空会话必须跳过」）/ **dsh-auth-cookies**（cookie 清理纯逻辑，22 例）/ review-panel（展示模型 64 + 控制器日志新鲜度 12 = **76 例**）/ file-panel（页签记忆 28 例 + open-with / tree-menu / image-zoom / editor-load-policy 模型 + 真实 NSWindow 面板场景）/ **projects-panel**（项目面板：模型 45 + 控制器无头 49 = **94 例**）/ **injected-scripts**（注入 dsh web 的 JS：脚本可解析 + `window.__dshX` 桥名与壳层调用对得上 + 不许出现会被 Swift 吃掉的转义）/ snapshot-panel（会话快照窗口模型）/ snapshot-rollback（快照/回退 CLI 端到端）/ l10n（键名 lint）/ terminal-emulator（薄封装）；terminal-panel 现覆盖头部固定标题 + `TerminalWorkspaceTabs` 工作区隔离 + 选中即复制；`.github/workflows/ci.yml` 的 swift job 与 `scripts/local-ci.sh` 跑同一清单——**新增套件必须两处都接**（8374736 / 407ccb1 的落地方式）
- **指令体系 v2（已合并，PR #31，2026-08-24）**：客户端斜杠指令重构——全局 `/help` `/ping` `/status`；工作区指令 `/workspaces`(`/wks`)、`/sessions`(`/ses`) 无内容列出 / 有内容切换（等同 `#wN`/`#sN`）、`/new [内容]` 统一回 `创建新会话 #sN (sessionId)`（无内容建占位 `New Session`（dsh 标题由 dsh web 按首条消息自动命名）等首条消息激活、有内容 prompt=内容并回推答案）；`/wks <裸数字>` 按下标切换工作区（2026-08-26）；删除 `/switch`；core 单测 **171 全绿**（钉钉落地后 186，2026-09-11 实测 **216 全绿**）。见 docs/channel-commands.md（清单）。
- **最近提交（2026-09-21，HEAD `b3451dd`）**：`f8d519c` 发布 **v1.16.0**（2026-09-21）——把此前已合并的技能面板（PR #50）、UX 反馈九条修复（PR #52）、面板配色统一（PR #51）、审查面板日志新鲜度（PR #49）等 `[Unreleased]` 条目**转正**（顶部 `[Unreleased]` 清为「暂无（v1.16.0 已发布；开发线已推进到 1.17.0）」），同 commit 同步 `README.md`（新增 ⌘R 重新认证自愈、目录树右键菜单、图片自适应缩放、大文件分块高亮、页签按工作区记忆、终端选中即复制与页签按 workspace 隔离、八面板灰阶配色段落与源码清单）、`CONTRIBUTING.md`（测试清单补 `tests/skills-panel/` 与 file/review 面板新增用例）与 `SECURITY.md`（支持版本表进 **1.16.x**、出 **1.14.x**，EOL 边界上移到 `< 1.15`）；`b3451dd`（`chore(version)`）发布后 fallback 推进到 **1.17.0**（BUILD 73）。
- **此前提交（2026-09-21，当时 HEAD `acf5783`）**：合并 **PR #52 `feature/ux-feedback-fixes`**——**使用问题清单（`docs/ux-feedback.md`，9 条）集中落地**，其中 8 条 ✅、#6 自愈路径已实现并留验收（长时运行回归待做）：
  - **Files**：① 目录树右键菜单（`FilePanelTreeMenu.swift` 纯模型 `TreeMenuModel.entries(hasRoot:hasRow:isRoot:isFile:)`——新建文件夹→新建文件 ｜ 重命名→删除 ｜ 在 Finder 中显示，文件上不提供新建、项目根置灰）+ `promptForNewItem`/`createTreeItem`/`renameTreePath`（页签跟随改名）/`trashTreeEntry`（移废纸篓 + 关页签）；② 头部换成共享控件 **`PanelMenuButton`**「打开项目 ▾ / 打开文件 ▾」（点击整颗即弹菜单、⌥ 点击走记住的方式；`OpenWithApps.swift` 的 `OpenWithCatalog` 列 面板/Finder/13 编辑器 IDE/8 终端，记忆键 `files.openProjectWith`；「打开文件」仅在选中文件时可用）；③ 图片预览改为**自适应 + 手动缩放**（`ImagePreviewView.swift` + 纯模型 `ImageZoom.swift`：5%–1600%、⌘+/−/0、⌘滚轮、捏合、双击、拖拽平移、`CenteringClipView` 居中 + 16pt 边距 + 固定 54×18 角标、fit 用 clip view frame 避免振荡）；④ 大文件**保留高亮改分块着色**（`EditorLoadPolicy.swift`：300 行/32 KB 分块让出主线程、0.6s 写文件稳定性窗口、后台读取 + generation 胜出、`CodeAttributedString.setLanguage(_:automaticallyHighlighting:)` 抑制重复高亮，安全阀只对 >4 万行 / >4 MB）；⑤ 目录树宽度跨关闭恢复（`TreeDividerSplitView` override `mouseDown` 才能感知分隔条 tracking loop，非拖拽重排一律纠正回记住的宽度，区间 160–420pt 且留 ≥240pt 内容区）；
  - **Terminal**：⑥ 滚动方向对齐 NSScrollView 语义（精确 delta 4 点 = 1 行 + 小数累加，鼠标一格 = 1 行）；⑦ 双击选词 / 三击选行、双击/三击后按词·行扩选、拖选结束**自动复制**（`terminal.autoCopy` 默认开，「设置 → 终端：选中文本即复制」；无选区 ⌘C 仍发 SIGINT）；⑧ 页签按 workspace 隔离（新增纯模型 `TerminalWorkspaceTabs.swift`：隐藏**不杀**会话、切回恢复选中、无项目目录的兜底页签全局可见；切到无终端的 workspace 且面板在屏上时自动开一个）；
  - **Shell**：⑨ ⌘R「重新加载页面」= 走 launch token **重新认证**（`reloadPageReauthenticating`），主框架 401 自动 `retryWithFreshAuth`、加载失败先自愈一次（自己 spawn 的 dsh web 死了则重拉），一次性守卫 + `logAuthCookieState` 诊断日志；`ServerManager.stop()` 清空 `entryURL`/`process`、新增 `isRunning`；
  - **测试**：新增 `tests/file-panel/` 的 open-with / tree-menu / image-zoom / editor-load-policy 四个模型测试与真实 NSWindow 的 panel-switch 场景、`tests/terminal-panel/` 的 workspace 隔离与选中即复制用例；2026-09-21 本机实测 `tests/file-panel/run.sh`、`tests/terminal-panel/run.sh` 均 EXIT=0。
- **此前提交（2026-09-21，HEAD `6411c2a`）**：合并 **PR #51 `feature/panel-surface-colors`**——**面板配色统一（UI color scheme）**：新增 `platforms/macos/src/PanelSurface.swift`（`PanelSurface` 面板底色 深 `#1B1B1C` / 浅 `#F9FAFB` + `PanelControl` 控件两档 常态 `#43454A`/`#FFFFFF`、高亮（hover / 按下 / 选中）`#353638`/`#F1F3F5`，六色全部取自 dsh web 的 `--dsw-static-neutral-bluish-*` 灰阶，**改色只改这一个文件**），八面板的 header / 内容区 / 列表底色与卡片、按钮、页签配色全部收敛到该单一来源（`0ef2874` 面板底色 → `5399c32` 控件两档 → `b2844e9` Tasks 面板列表内容区未吃到面板底色的修正）；`DynamicFillView.Kind` 由 `.window`/`.control` 收敛为 `.panel`/`.custom`，新增 `PanelTabButton`（文件 / 预览 / 终端页签），`CustomIconButton` 删掉 `showsBackground`、`HoverButton` 常态即上底色；**取色规则**：自绘与 CALayer 用显式分档，系统视图用 `PanelSurface.dynamic`，`NSTableView`/`NSOutlineView` 必须显式设 `backgroundColor`；方案文档 `docs/ui-color-scheme.md`（令牌表 / 层级 / 应用映射 / 系统绘制不覆盖清单 / 落地规则 / 验证），AGENTS.md 约定 6 同步；渲染回归 `tests/skills-panel/render-tests.swift` 断言 6 个令牌与真实像素，`tests/{browser,file,review,skills}-panel/run.sh` 编译清单加入 `PanelSurface.swift`。
- **此前提交（2026-09-17，当时 HEAD `a704ba1`）**：合并 **PR #50 `feature/skills-manager`**——**技能面板（Skills / ⌥⌘S）落地**：新增 `SkillsPanel.swift`（1667 行，右栏第 8 面板：已安装/可安装两页签 + registry 管理页 + 候选卡 hover/详情）、`SkillsCore.swift`（742 行纯 Foundation：`SkillFrontmatterIO` 只增删改 `user-invocable`/`disable-model-invocation` 两行、切回基线即删键字节还原；`SkillRoots`/`SkillScanner` 扫 dsh 四个技能根（`.dsh/skills` 100 > `.agents/skills` 200 > `$DSH_HOME/skills` 400 > `~/.agents/skills` 500）按 rank 去重标「被遮蔽」与四档级别；`SkillStore` 读写 `$DSH_HOME/shell/skills.json`）、`SkillSources.swift`（919 行：地址解析 8 形态、registry 清单/搜索/probe、git clone→codeload tarball→well-known HTTP 取回、安装/移除服务，传输可注入）+ `tests/skills-panel/`（121 例：模型 + 控制器冒烟 + **真实 `DynamicFillView`/`HeaderLabel` 绘制回归**）+ `docs/skills-manager-design.md`；**内置技能只读**（不改 `SkillInstaller.swift`，字节断言不变）、共享级可改开关不可移除；改开关/装/删后 `nudgeDSHWebCaches()` 派发 offline→online 让 dsh 客户端重连、清掉会话级技能目录缓存（1.5s 节流，`DSH_SKILLS_NO_NUDGE=1` 关闭）。同期 `2119bb1`（`fix(panel)`）修 `DynamicFillView` 越界填充（脏矩形大于 bounds 时刷掉层级更低的兄弟视图 → 技能面板头部标题/标签整条消失），`6dd6c0e` 修滚动后 hover 残留（`SkillHoverResolver` 按当前指针位置重算）。此前 `df7c163` 发布 **v1.15.0**（[Unreleased] 转正 + SECURITY/README/CONTRIBUTING 同步：面板数七个→八个、默认渲染模式 windowed、测试套件清单补齐），`d9affe1` 发布后 fallback 推进到 **1.16.0**（BUILD 72）。
- **近期提交（2026-09-17）**：合并 **PR #49 `fix/review-audit-staleness`**——**审查面板审计结果随会话日志增量刷新**：审计缓存原来只按 sessionId，而会话日志是**活文档**（dsh 每落盘一批追加一个独立可解压的 Zstandard 帧，只增不减），新建会话一诞生（成为 dsh web 当前会话）就被审一次、那会儿日志里只有会话头，「0 文件 / 无变更」被**永久钉住**。现改为按**日志身份** `ReviewLogStamp`（size + mtime，`ReviewLogModel.swift`）失效：戳在审计前打、读完落盘（进行中追加的帧仍比戳新，下个 tick 再读；失败同样打戳不反复重试），日志路径未知时判「无法判断」保留缓存；打开面板即重列（`ensureLoaded()` 不再只列一次、重列不擦内容），面板**在屏上**时每 5 s 给已展开会话 `stat` 一次、只有真的变了才跑 `review audit`（收起即停——可见性判定为 `superview != nil && window != nil && frame.width > 1`）；新增 `tests/review-panel/controller-tests.swift`（无头驱动真控制器 + 假 core CLI，12 例，对修复前代码 6 例 FAIL），整套 76 例。
- **最近提交（2026-09-13，历史）**：`8374736`（`fix(cookies)`）——**修「启动后 WebView 报 Failed to load plugins」**：dsh ≥ 0.1.2 的浏览器 cookie 名由 authority（`127.0.0.1:<port>`）派生（`"dsh-auth-" + base64url(sha256(authority))`），而 cookie 本身**不区分端口**、WKWebView 存储按 bundle id 持久化（30 天 TTL），壳层每次自拉起新端口的 dsh web 就多留一只（实测 68 只 / ~226 B），且永不复用覆盖；链路唯一被压垮的是 **client-modules 的 application batch**（45 插件拼成同一条 ~2.1 KB combo URL），累积 `Cookie:` 头过 ~14.1 KB（第 63 只）后请求整体超 node 默认 16 KiB header cap → **431**（空 body）→ `<script src>` error → 界面插件全挂（外壳与 bootstrap 正常）。**处置**：新增 `platforms/macos/src/DshWebCookieJanitor.swift`（复刻 cookie 名派生；启动加载入口 URL **前**清掉所有非本次 authority 的 `dsh-auth-*`、退出 `purgeAll` 清本次留下的；只碰该前缀，localStorage / `$DSH_HOME` / CEF profile 不动）+ spawn dsh web 时给 `NODE_OPTIONS` 追加 `--max-http-header-size=65536`（已显式设置则原样保留）；新增 `tests/dsh-auth-cookies/`（22 例，真实 cookie 名做向量），接入 `scripts/local-ci.sh` 与 CI swift job；文档 docs/dsh-version-impact.md §6.3（R6）。同日 `407ccb1`（`fix(settings)`）——**补旧 UserDefaults 一次性迁移 + 渲染默认改回 `windowed`**：1.14.0 把壳层设置搬进 `$DSH_HOME/shell/config.json` 时没迁移旧值，用户设过的 `browserRenderMode=windowed` 失效、静默回落到当时损坏的 OSR（升级 1.14.0 后面板空白的另一半根因）；现 `ShellConfig.loadIfNeeded`（持锁）一次性把 16 个壳层自有旧键搬进 config.json（只搬本文件尚无取值的键、`legacyUserDefaultsMigratedAt` 保证只做一次、直接原子写避免自锁），`main.swift` 渲染默认改回 `windowed`（显式设 `osr` 才走离屏），新增 `tests/shell-config/`（13 例）并入两处 CI。`9a39fe0`（`fix(browser)`）OSR 帧改画在可见的 `pageView` 层 + 菜单锚点以鼠标屏幕坐标为准 + 帧按 `browserId` 派发；`9c29ad0` 补 docs/browser-blank-panel-fix.md；`e617bf0` 同步 wiki；`f5a763d` 合并 **PR #48 `fix/browser-blank-panel`**。
- **最近提交（2026-09-12，历史）**：`e754c2c`（合并 **PR #44 `feature/review-panel`**）——**审查面板（只读变更审计）落地**：core 新增 `core/lib/review-log.js`（dsh 会话日志 Zstandard **多帧**扫描/解码 + 三类记录合并 + turn 归属 + shell 启发式 + 诊断，CLI `review sessions|audit|audit-file`）、面板 `ReviewPanel.swift`（右栏第七面板，⌥⌘R，会话→对话→文件→变更 树、按需审计 + 缓存、跟随 dsh web 会话、只读）与 `ReviewLogModel.swift`（纯 Foundation 展示模型）、`tests/review-panel/`（64 项）+ `core/tests/review-log.test.js`（17 项，无 zstd 时 3 项 skip）；设计/覆盖矩阵见 docs/review-panel-design.md。同期 `ebac11a` 修掉用例泄漏 runner 导致的整套挂死（`node --test --test-timeout=60000`，ci.yml / nightly.yml / local-ci.sh / core/package.json 全量对齐）；`5348c0d` 发布 **v1.14.0**（[Unreleased] 转正 + SECURITY 支持版本表/README/CONTRIBUTING 同步），`2996b06` 发布后 fallback 推进到 **1.15.0**（BUILD 71）。此前（2026-09-11）：合并 **PR #42 `feature/dsh-upgrade-staged`**——**dsh 0.1.2 兼容收尾（R1–R4）**：① 原生 RPC（`platforms/macos/src/DshWebRPC.swift`）双面端点 + launch-token cookie，wiki / issue-runner / 会话目录全部接通 0.1.2；② 注入脚本 `sessionOpenerScript` 改为**运行时双面**（先 0.1.2 斜杠形状、失败回退点号——此前写死单版本形状，0.1.2 与 0.1.1 各坏一次），`DSH_UI_DEBUG=1` 新增注入桥自检 `dsh injected bridges: {tracker, opener, preview, rows}`；③ **dsh 私有 workspace 存储读取加护栏**：core（`core/lib/workspace-store.js`）与 Swift（`DshWorkspaceStore`）双侧校验 `unit.name/version`（`workspace` v2），读不懂仍尽力解析但经 `[workspace-store]` 报诊断（文件缺失保持安静），`main.swift` 的 `persistedWorkspacePath` 收口到 `DshWorkspaceStore`（三份解析→两份），补 core 4 + Swift 4 用例；④ 外部已启动的 0.1.2 实例**按设计不复用**（token 每进程随机不可得；同 `DSH_HOME` 数据本就共享），该判断写入 `app.log`；core 单测 **216 全绿**。详见 docs/dsh-version-impact.md（§6.1 R3 / §6.2 R4 详解）。此前 **最近提交（2026-08-26）**：`aad6daa`（`docs(readme)`，合并 **PR #39 `feature/channel-dingtalk-stream`**）——**钉钉原生适配器落地（M4 提前）**：core 新增 `dingtalk-stream-transport.js`（DWClient Stream 长连接，TOPIC_ROBOT 收消息归一化 + sessionWebhook 回发 + access-token 缓存 + msgId 去重）、`dingtalk.js`（ChannelAdapter，platform=dingtalk）、`dingtalk-access.js`（`/bind` owner-binding 安全门：未绑定拒绝所有人、绑定状态存 `~/.dsh/channels/<channelId>.binding.json`）、`dingtalk-device.js`（device-code 扫码注册）+ `channel login-dingtalk` CLI；`runDingTalkChannel` 镜像 runWeixinChannel 编排、复用同一 channel store，面板项目视图零改动；面板钉钉扫码向导（已配置不重复扫码/重开恢复 /bind）、微信绑定页展示已配置状态 + 显式「重新登录」、全局配置 **unbind** 解绑；`/wks <裸数字>` 下标切换（镜像 /ses <N>）；系统/指令消息落通道级全局桶；core 单测 **186 全绿**（较 171 新增 dingtalk 15 用例）。设计见 docs/channel-dingtalk-stream.md（与微信**同构**的 core 适配器路线，非官方插件托管）。此前
- **最近提交（2026-08-25）**：`0e70d4d`（合并 **PR #36 `fix/upgrade-and-restart`**）——**自动升级修复**：① 给 npm 升级子进程前置注入内置 node 目录到 `PATH`（GUI 启动的 App 继承 launchd 精简 PATH 通常无 `node`，npm 依赖包 lifecycle 脚本如 `@deepseek-ai/dsh-subprocess-local` 的 postinstall `node ensure-spawn-helper.mjs` 经 shell 按 `PATH` 找 `node`，此前报 exit 127、升级中断）；② 新增 `restartServerAfterUpgrade()`——自动/手动升级成功后停止 App 自拉起的服务并重拉 + 重载 WebView 使新 dsh 立即生效（复用的外部 3080 服务下次启动生效；覆盖首次启动失败场景）。此前 **PR #35 `feature/sync-dsh-rc2`**：`37090bc` 默认 dsh 升级到 `@deepseek-ai/dsh@0.1.1-rc.2`；`6671bd2` 发布 **v1.13.0**（[Unreleased] 转正 + SECURITY/CONTRIBUTING 同步），`882e211` 发布后 fallback 推进到 1.14.0（BUILD 70）；另有 patch tag `v1.13.1`（release 分支，重启修复细化，未合入 main）。此前
- **最近提交（2026-08-24，历史）**：`2ea0d81`（`docs(readme)`，合并 **PR #30 `feature/channel-project-switch`**）——**Channel「项目开关」落地**：通道↔项目（workspace）启用关联存全局 `~/.dsh/channels/<channelId>.workspaces.json`（project=workspace，出现即启用该通道；不再写项目内 `.dsh/channels.json`），`ChannelPanel` 移除 `refs`/`saveRefs` 作启用来源、新增 `isChannelEnabled`/`setChannelEnabled`（旧 refs 仅一次性惰性迁移播种），`channel run` 新增 `--project-root`，`main.swift` `startChannelRunner` 删除强制补 ref 并追加 `--project-root`；**门控**：普通消息/`/new` 路由到未启用 workspace、`#wN/#sN`、`/sessions`、`/switch` → 回「该项目未启用该通道」、不建会话，`/workspaces` 只列已启用，`/help` `/ping` `/status` 放行；L10n 新增 `channel.notEnabledInProject`；core 单测 **166 全绿**（较上次 157 新增 project-switch/workspaces 启用用例）。此前 `1ef594c`（`docs(readme)`）与 `0321a9c`（合并 **PR #28 `feature/channel-association`**）——**Channel–Message–Session 关联模型落地**：会话复用 A（sessionDriver.run 复用 event.sessionId）/ 路由统一 B（resolveRefBinding：conversation/keyword 显式绑定 > workspace-tag 兜底）/ 工作区归属 C（普通消息以 workspaceId 创建/复用会话）/ **存储全局化 D**（createChannelSessions 改按 channel 作用域存全局 `~/.dsh/channels/`，sessions/workspaces/分桶 messages，项目目录不再产生消息/会话文件）/ **面板项目视图 E**（新增 `platforms/macos/src/ChannelStoreReader.swift` 纯 Foundation 读全局 store，ChannelPanel 会话行展开显示消息气泡，无头单测 `tests/channel-panel/`）；**异步应答 + 忙门 + sendTyping**（微信原生「正在输入…」替代「处理中」文字，后台生成 + 结果回推，同 conversation 在途时回「请等待」不入队）；runner stdout/stderr 路由到 `~/Library/Logs/oh-my-dsh/channel-runner-<channelId>.log`；core 单测 **157 全绿**（基线 148 + association/busy/重写 sessions）。此前 **PR #27 `feature/builtin-skills-global`**：`e0fd1f8`（2026-08-22）——**内置 Skill 全局化 + 重命名**：新增 `platforms/macos/src/SkillInstaller.swift`，三个面板配套 skill 改由 **App 启动时安装到全局 `$DSH_HOME/skills/`**（缺失即装、受管下自动覆盖更新、用户改过不覆盖），并重命名 `shell-browser`→`web-dev-tools`（浏览器面板）/ `repo-wiki`→`repo-knowledge`（Repo Wiki）/ `issue-fix`→`issue-resolve`（IssueRunner）；启动时自动把旧名迁移到新名；移除面板「按仓库安装」逻辑（`WikiSkill.ensureInstalled` / `ensureIssueFixSkillInstalled` 删除）；新增 `tests/skills/` 无头单测（含内嵌 SKILL.md 与仓库副本字节一致断言）；skill frontmatter 用合法键（省略弃用的 `modelInvocable`/`userInvocable`，新增 `user-invocable: false` 表「仅 model 可调用」）。同 PR 落地 **macOS 源码清单单一事实来源**：新增 `platforms/macos/swift-sources.sh`（glob 自动收录 `src/*.swift` + `vendor/Highlightr/*`，排除独立工具 `MakeIcon.swift`），`build-app.sh` / `scripts/local-ci.sh` / `ci.yml` 三方共用，新增 Swift 文件不再逐个登记，消除「新增文件遗漏 local-ci」问题，见 [skill-installer](modules/skill-installer.md) 与 [build-scripts](modules/build-scripts.md)。此前 PR #26 `fix/single-instance`（**单实例约束**：启动按 bundle id 检测已有实例则聚焦并退出，防双实例争抢 CEF profile 致 Chromium 异常退出；**开发版** `DSH_DEV_BUILD=1` / `scripts/local-ci.sh dev`：Info.plist 写 `DSHDevBuild=1`、独立 CEF profile `~/.dsh/browser-dev`、跳过单实例退出，可与已安装正式版并存测试）与 PR #25 `fix/release-smoothing`（**发布平滑**：发布流程补 READM... (line truncated to 2000 chars)
- 工作区版本号：`1.17.0`（fallback，BUILD 73；最新发布 tag **`v1.16.0`**（2026-09-21 发布：技能面板 + UX 反馈修复转正，README/CONTRIBUTING/SECURITY 同步），另有 `v1.13.1`（release 分支 patch）；主版本发布后 fallback 立即推进到下一 minor，见 [conventions](conventions.md)）
- 仓库新增文档：`docs/ui-color-scheme.md`（**壳层 UI 配色方案**：设计原则、6 个表面色令牌与 dsh `neutral bluish` 灰阶对应表、两套取色 API、层级与方向、各面板应用映射（谁用哪个令牌）、语义色清单、系统绘制不覆盖清单、落地规则与验证方式、待定事项；单一事实来源 `platforms/macos/src/PanelSurface.swift`，2026-09-17 随 PR #51 落地）、`docs/channel-design.md`（Channel 统一抽象设计：平台适配器 / 状态机 / 路由 / 配置模型）、`docs/channel-commands.md`（已实现指令清单，改动须同步维护）、`docs/channel-status.md`（面板 + core 完成状态总览）、`docs/channel-storage.md`（消息/会话存储全局化设计，**已实现**，2026-08-22 落地于 `~/.dsh/channels/`）、`docs/channel-project-switch.md`（**「项目开关」设计**：全局 workspaces.json 关联 + 未启用门控，**已实现**，2026-08-23 随 PR #30 落地）`docs/channel-association-model.md`（Channel–Message–Session 关联模型：会话复用 A / 路由统一 B / 工作区归属 C / 存储全局化 D / 面板项目视图 E）、`docs/channel-dingtalk-stream.md`（**钉钉 dingtalk-stream 原生适配器设计，已实现** 2026-08-26）、`docs/channel-ui-commands.md`（面板 UI + 指令设计）、`docs/channel-issues.md`（重复回复根因排查）；`docs/productization.md`（产品化方案：P0 已达成 → P1 开源/CI → P2 Windows → P3 Linux → P4 生态 → F Apple 生态暂缓，见 [overview](overview.md)）；`docs/git-workflow.md`（统一分支与发布规范：main 只合并/只打主版本，feature/fix 走 PR，已发布 bug 走 release/X.Y + patch tag + cherry-pick 回 main，见 [conventions](conventions.md) 与 [tasks](tasks.md)）；`docs/milestones/`（M1 产品化基础 … M5 Apple 生态 5 份里程碑目标文档，README「目录」收录）；`docs/issue-runner-design.md`（任务面板设计 + 远程驱动预留 + 关联索引章节）；`docs/devtools-drag-fix.md`（DevTools 拖动导致 CEF 视图上移问题的分析与修复）、`docs/review-panel-design.md`（**审查面板设计**：目标/非目标、三类会话日志记录、覆盖矩阵、为何审计逻辑放 core（zstd 多帧）、CLI 契约、面板行为与已知边界，2026-09-12 随 PR #44 落地；2026-09-17 补「按需审计 + 只认同一份日志」与新触发点）、`docs/skills-manager-design.md`（**技能面板设计**：四档级别与可写性、dsh 侧契约（rank/键名/chokidar）、registry 模型与热门视图、地址形态、开关可逆写法、客户端缓存失效、磁盘布局与失败模式、明确不做，2026-09-17 随 PR #50 落地）
- 发布/CI 工具链：版本单一来源 `scripts/version.sh`（git tag 驱动）；本机发布 `scripts/local-release.sh`（含 `pack` 只打包子命令）；`scripts/release-checksums.sh`（SHA-256SUMS + release notes）+ `scripts/github-publish.sh`（gh CLI / curl API 兜底）发布；`Jenkinsfile` Jenkins 打包发布；release.yml 新增 prepare 前置 job 预编译双架构 CEF、runtime/CEF 缓存按架构分目录（`.cache/runtime/<arch>`、`.cache/cef-built-<arch>`）——详见 [build-scripts](modules/build-scripts.md) 与 [tasks](tasks.md)
- 任务关联索引：`.dsh/tasks/index.json`（issue→branch→PR→state，随仓库提交）+ `local.json`（sessionId，gitignore），实现 `core/lib/tasks.js`（Node）+ `IssueRunnerPanel.swift` 的 `TaskIndex`（Swift），见 [data-model](data-model.md) 与 [issue-runner-panel](modules/issue-runner-panel.md)
- GitHub token：按仓库作用域——Keychain 专属 + 文件 `~/.dsh/tokens/<owner>-<repo>` 双写（chmod 600），回退 Keychain 通用 + `~/.dsh/gh-token`（外部工具/代理共用）；读取**优先文件**、Keychain 兜底（免 Keychain 弹密码，88f0255），见 [issue-runner-panel](modules/issue-runner-panel.md)
- Channel 配置与状态：账号/token `~/.dsh/channels/<id>.json`（chmod 600，文件优先 + Keychain 兜底；钉钉含 AppKey/AppSecret）、**钉钉管理员绑定 `~/.dsh/channels/<channelId>.binding.json`**（chmod 600，/bind owner-binding 安全门）、通道级状态 `~/.dsh/channels/<id>.state.json`（lastWorkspace/会话映射/activeSession，重启可恢复）、**「项目开关」启用关联 `~/.dsh/channels/<channelId>.workspaces.json`**（PR #30，project=workspace，出现即启用该通道）；旧项目引用 `.dsh/channels.json` 不再作为启用来源（迁移到 `.dsh/channels/channels.json` 并提交为待办）；会话映射/消息归档**已全局化**到 `~/.dsh/channels/`（按 channelId/workspaceKey/sessionId 分桶；系统/指令消息落通道级全局桶），见 [channel-panel](modules/channel-panel.md) 与 [data-model](data-model.md)
- 任务面板工作区跟随：会话/工作区切换时壳层**先更新 `ProjectDirectory`** 再**无条件**调用 `tasksPanel.workspaceChanged()`（fetch cwd 失败也触发）——面板以 workspacePath 为**权威**（GitHub 仓库 → 显示 issues；非 GitHub → 诚实空态、不替换其他工作区；仅启动早期回退 workspace.list 扫描，≤10 次重试），见 [issue-runner-panel](modules/issue-runner-panel.md) 与 [architecture](architecture.md)
- Wiki 提交：更新完成后由维护代理按指令执行 `git add .dsh/wiki` + commit（不 push，message 概括实际变更；repo-knowledge skill 现行规则已不含提交步骤）；`WikiAutoCommit` 兜底（代理未提交时），见 [wiki-panel](modules/wiki-panel.md)

## 维护约定

- 本知识库由 dsh 代理按 `repo-knowledge` skill（`.dsh/skills/repo-knowledge/SKILL.md`）维护；
- 增量更新只重写 `sources` 命中变更的页面；`manual: true` 页面绝不改写；
- 内容只写可证实事实；`.env*`/密钥/口令一律不收录。

最后生成时间：2026-09-21T09:43:26Z